# Paved-road consumer: drop this in your repo at .github/workflows/terraform.yml
# and pin the catalog version you want.
name: terraform

on:
  pull_request:
  push:
    branches: [main]

permissions:
  id-token: write   # required for keyless OIDC
  contents: read

jobs:
  terraform:
    uses: Uaena1711/iac-github/.github/workflows/terraform.yml@terraform/v1
    with:
      workspaces_root: envs
      default_region: us-east-1
    # No `secrets: inherit` — the flow is fully keyless (OIDC). Pass secrets explicitly
    # only if you add a step that needs one.
