# Per-stack identity for the prod workspace. PLACEHOLDERS — replace locally; do NOT commit
# real ARNs / bucket names / account ids.
AWS_ROLE_ARN=arn:aws:iam::ACCOUNT_ID:role/REPLACE_ME
AWS_REGION=us-east-1
AWS_STATE_BUCKET=REPLACE_ME
AWS_STATE_KMS_KEY=REPLACE_ME
